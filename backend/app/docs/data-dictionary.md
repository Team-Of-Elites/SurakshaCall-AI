<!-- 
This explains what each database field means.

For example:

sessions.id
→ Unique ID for a call session

sessions.started_at
→ Time when session started

risk_snapshots.risk_score
→ Current calculated risk score
 -->